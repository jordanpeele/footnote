.env.local
.env
.vercel
node_modules/
.DS_Store
*.log
.claude/
.playwright-mcp/
tools/street/test-audio-5min.mp3
tools/street/scratch-audio/
docs/STATUS_*
tools/adjudicate/queue.json
tools/adjudicate/graduations.json
daysprint/
# Synthetic fixture audio: sidecars (ground truth) commit; the .wav is large and
# deterministically regenerable (npm run synth:fixtures). The daysprint/ ignore above
# already covers this path — this line documents intent for when fixtures graduate to a
# tracked location.
tools/synthetic/fixtures/*.wav
