# Footnote relay front-door tripwire — KNOWN-EXPECTED ingest sources.
#
# The tripwire (relay-tripwire.sh) records any SRTLA registration whose source IP is NOT
# matched here. Operator: EDIT THIS FILE to add your carriers/home as they rotate. One
# entry per line: a bare IPv4 (treated as /32) or a CIDR. Inline "# ..." comments allowed.
# Deployed to the relay at /etc/footnote/relay-allowlist.conf. Changes take effect on the
# next unknown-source evaluation (no restart needed — the tripwire re-reads per hit).
#
# Seeded from run2 (2026-08-15) forensics — the operator's observed carrier/home IPs:

76.32.135.77          # home ISP (observed run2; rotates — update when your WAN IP changes)
166.199.0.0/16        # Verizon cellular (166.199.x seen bonded in run2)

# --- add other cell carriers / hotspots below as you observe them ---
# A registration from anything NOT covered above is logged as an unknown source and
# surfaced at :8080 (recent_unknown_sources) + in the Mac preflight WARN check.
