# FOOTNOTE — STATUS REPORT FOR ORCHESTRATOR · Sun 2026-08-10

*Prepared by the Claude Code execution session after sprint-02 close + tonight's
vertical recording session. Repo: github.com/jordanpeele/footnote (public, MIT).
Verified at write time: main == origin/main @ 15beb23 · CI green · 111 tests ·
prod healthy at footnote-live.vercel.app.*

## Position

Sprint 02 (latency + polish) is **closed and fully reported**; tonight's short
vertical recording session cleared two additional open items on top of it.

## Sprint-02 outcome (one line)

The 3.5s machine floor is **proven vendor-bound** — every sanctioned diet lever
was benched and came back noise or accuracy-priced (all reverted per the
accuracy-wins rule; ~$3 of bench evidence in `tools/bench/`, full tables in
`docs/LATENCY_LEDGER.md`) — while the on-air product got its broadcast polish:
240ms/200ms motion, per-verdict accents (NeedsContext violet), correction-
dominant typography verified to 390px phone scale, curated source display
names, PRIMARY SOURCE tier tag, multi-source receipts, self-hosted fonts
(the broadcast surface now makes zero third-party calls).
**Gate-conflict log: EMPTY by measurement** — gates cost 1ms; latency work
never had a reason to approach them.

## Tonight's session — two closures

- **F2 dedupe: FIELD-FIRED.** A live repeated claim was suppressed as
  `duplicate_claim`; exactly one card aired. Every round-5 fix has now proven
  itself on camera (merge 3-claim recovery, grounding live kill, dedupe
  suppression, overlay wake, watermark, portrait).
- **L2 resolved by live A/B:** `?ep=200` made finals **140ms slower**
  (704 vs 564ms p50) for a ~33% split-rate reduction — but the P5-B merge
  already recovers splits for free, so **stock endpointing wins both axes**.
  Decision recorded in the ledger: no default change; the knob stays as an
  experiment hatch. L2 is off the board.
- Session results: 3/3 correct verdicts (incl. a True wearing the new
  PRIMARY SOURCE dress), spoken→screen 3.1–4.3s, air→render p50 306ms.
  The operator now has recorded footage of the new chyron working live.

## Cumulative field ledger

Three sessions · 63 checks · **0 wrong verdicts aired.**

## Decision queue (two items retired tonight; priorities unchanged)

1. **VERIFY ARCHITECTURE** — the only remaining latency lever (faster model
   tier vs parallel evidence+commit), each carrying a full D15 calibration
   bill. Needs an orchestrator packet; sprint-02's bench harnesses are ready
   to score whichever direction is chosen.
2. **ADJUDICATION SITTING** (`eval/ADJUDICATION_QUEUE.md`, ~60–90 min) —
   still the binding constraint on ALL eligibility work; three calibration
   runs have now stalled on the same unadjudicated inversions.
3. **MEET-CALL CAPTURE TEST** — last pre-street item; 3-step setup ready.
4. **THE ANNOUNCEMENT** — still unposted (0 stars/forks confirms). Everything
   it links to is now field-proven and polished; purely operator timing.

## Standing risks / notes

None new. P5-C (two-step verifier) stays dark by its own eval; promotion
remains an orchestrator decision. No editorial gate was touched by any
sprint-02 change. Street posture unchanged: veto-everything, /op is the
authority, machine floor ~3.5s.
